[project]
name = "hermes-book-automation"
version = "0.1.0"
description = "Hermes autonomous textbook production pipeline"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
